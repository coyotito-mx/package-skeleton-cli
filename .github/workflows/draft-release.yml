name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          tools: composer

      - name: Install Dependencies
        run: composer install --no-dev --no-interaction --no-progress --optimize-autoloader

      - name: Build CLI
        run: php skeleton app:build --no-interaction --build-version="${{ github.ref_name }}" skeleton

      - name: Archive CLI
        run: |
          cd builds &&
          zip skeleton.zip skeleton &&
          tar -czf skeleton.tar.gz skeleton

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: skeleton
          path: builds/*

  build-draft-release:
    runs-on: ubuntu-latest
    needs: build

    permissions:
      contents: write

    steps:
      - name: Download Artifacts
        id: download_artifact
        uses: actions/download-artifact@v3
        with:
          name: skeleton

      - name: Create Draft Release
        id: github_draft_release
        uses: release-drafter/release-drafter@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

      - name: Upload Artifacts
        id: upload_artifact
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ steps.github_draft_release.outputs.name }}
          body: ${{ steps.github_draft_release.outputs.body }}
          files: |
            builds/skeleton
            builds/skeleton.zip
            builds/skeleton.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
